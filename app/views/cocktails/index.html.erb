<div class='banner' style="background-image: url('<%= image_path 'background-image.jpg' %>');">
</div>

<h1>Cocktails</h1>

  <tbody>
    <% @cocktails.each do |cocktail| %>
      <tr>
        <td><%= link_to cocktail.name, cocktail %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= link_to 'New Cocktail', new_cocktail_path, class: 'btn btn-primary' %>

<%=  %>