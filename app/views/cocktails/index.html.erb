<div style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('<%= image_path 'background-image.jpg'  %>');">
</div>

<h1>Cocktails</h1>

  <tbody>
    <% @cocktails.each do |cocktail| %>
      <tr>
        <td><%= cocktail.name %></td>
        <td><%= link_to 'Show', cocktail %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= link_to 'New Cocktail', new_cocktail_path, class: 'btn btn-primary' %>

<%=  %>